# The "Sponsor this project" panel on the repository page.
#
# Ko-fi only. GitHub Sponsors pays out through Stripe, which does not operate
# in Vietnam, so a `github:` entry here would render a button that cannot
# actually receive anything.
#
# Requires the "Sponsorships" checkbox under Settings -> General -> Features;
# this file alone does not display the panel. Must live on the default branch,
# which is `develop` here, not `main`. An invalid entry hides the whole panel,
# so add `github:` only once a Sponsors profile actually exists.
ko_fi: hhoangg
